import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.dangeo13.mindcanvas',
  appName: 'com.example.mindcanvas',
  webDir: 'dist'
};

export default config;
